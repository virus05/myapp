version: '3.9'

services:
  web:
    image: myapp:latest
    container_name: myapp
    ports:
      - "5000:5000"
    networks:
      - myapp_net
    volumes:
      - myapp_data:/data

networks:
  myapp_net:

volumes:
  myapp_data:
